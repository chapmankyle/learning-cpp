#include <iostream>

#include "io.h"
#include "guess.h"
#include "quizzes.h"

/*
 * Main function.
 */
int main() {
	// when debugging using printing, use std::cerr instead of std::cout
	// since the latter is buffered

	// chap4quiz3();
	// chap6quiz1();
	// chap6quiz3();

	start();

	return 0;
}
